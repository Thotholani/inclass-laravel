import StudentNavigation from '@/components/student-navigation';

export default function StudentLessonsPage() {
    return <></>;
}

StudentLessonsPage.layout = (page: React.ReactNode) => <StudentNavigation children={page} />;
